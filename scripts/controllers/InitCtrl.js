; 'use strict';

blAppControllers.controller('InitCtrl', ['$scope', '$location', '$log', '$http', '$state',
    function($scope, $location, $log, $http, $state) {

        $log.debug('In Init Controller!');

    }]);